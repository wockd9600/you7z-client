import SettingsPanel from "./SettingsPanel";
import UserPanel from "./UserPanel";

const GameBox = () => {
    

    return (
        <article>
            <UserPanel />
            <SettingsPanel />
        </article>
    );
};

export default GameBox;
