export enum DropdownOption {
    SET_NAME,
    // MY_PLAYLIST,
    CREATE_PLAYLIST,
    LOGOUT,
}

export enum PlaylistModalType {
    POPULAR,
    // MY,
    CREATED,
    // MY_WITH_CREATED,
}

export enum GameStatus {
    NOT_STARTED = 0,
    STARTED = 1,
    IS_GAMING = 2,
    IS_WAITING = 3,
}
