export interface Song {
    youtubeLink: string;
    startTime: string;
    answer: string;
    description?: string;
}

export interface VideoData {
    title: string;
    duration: string;
    thumbnail: string;
}
